"""Pure-Python logic: parsing, writing, keys, connection tests.

Nothing in this package may import `gi`. A test enforces it, so the logic
stays runnable without a display and a future CLI stays possible.
"""
