"""GTK4 + libadwaita widgets.

Two rules hold this layer together: it never parses or writes config text by
hand (that is `parvussh.core`), and it never contains a literal user-visible
string (that is `parvussh.i18n`).
"""
